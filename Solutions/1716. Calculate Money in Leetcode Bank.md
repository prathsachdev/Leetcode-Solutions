<!-- problem:start -->

# [1716. Calculate Money in Leetcode Bank](https://leetcode.com/problems/calculate-money-in-leetcode-bank)

## Description

<!-- description:start -->

<p>Hercy wants to save money for his first car. He puts money in the Leetcode&nbsp;bank <strong>every day</strong>.</p>

<p>He starts by putting in <code>$1</code> on Monday, the first day. Every day from Tuesday to Sunday, he will put in <code>$1</code> more than the day before. On every subsequent Monday, he will put in <code>$1</code> more than the <strong>previous Monday</strong>.<span style="display: none;"> </span></p>

<p>Given <code>n</code>, return <em>the total amount of money he will have in the Leetcode bank at the end of the </em><code>n<sup>th</sup></code><em> day.</em></p>
<p>&nbsp;</p>

<p><strong class="example">Example 1:</strong></p>
<pre>
<strong>Input:</strong> n = 4
<strong>Output:</strong> 10
<strong>Explanation:</strong>&nbsp;After the 4<sup>th</sup> day, the total is 1 + 2 + 3 + 4 = 10.
</pre>

<p><strong class="example">Example 2:</strong></p>
<pre>
<strong>Input:</strong> n = 10
<strong>Output:</strong> 37
<strong>Explanation:</strong>&nbsp;After the 10<sup>th</sup> day, the total is (1 + 2 + 3 + 4 + 5 + 6 + 7) + (2 + 3 + 4) = 37. Notice that on the 2<sup>nd</sup> Monday, Hercy only puts in $2.
</pre>

<p><strong class="example">Example 3:</strong></p>
<pre>
<strong>Input:</strong> n = 20
<strong>Output:</strong> 96
<strong>Explanation:</strong>&nbsp;After the 20<sup>th</sup> day, the total is (1 + 2 + 3 + 4 + 5 + 6 + 7) + (2 + 3 + 4 + 5 + 6 + 7 + 8) + (3 + 4 + 5 + 6 + 7 + 8) = 96.
</pre>
<p>&nbsp;</p>

<p><strong>Constraints:</strong></p>
<ul>
    <li><code>1 &lt;= n &lt;= 1000</code></li>
</ul>
<p>&nbsp;</p>

<!-- description:end -->

## Solutions

<!-- solution:start -->

### **Approach 1: Math**

According to the problem description, the deposit situation for each week is as follows:

```bash
Week 1: 1, 2, 3, 4, 5, 6, 7 (Sum = 28)
Week 2: 2, 3, 4, 5, 6, 7, 8 (Sum = 35)
Week 3: 3, 4, 5, 6, 7, 8, 9 (Sum = 42)
...
Week k: k, k+1, k+2, k+3, k+4, k+5, k+6
```

Given $n$ days of deposits, the number of complete weeks is $fullWeeks = \lfloor n / 7 \rfloor$, and the remaining days is $rmngDays = n \mod 7$.

The total deposit for the complete $fullWeeks$ weeks can be calculated using the arithmetic sequence sum formula:

$$
sum1 = \frac{fullWeeks}{2} \times (28 + (28 + 7 \times (fullWeeks - 1)))
$$

The total deposit for the remaining $rmngDays$ days can also be calculated using the arithmetic sequence sum formula:

$$
sum2 = \frac{rmngDays}{2} \times ((fullWeeks + 1) + (fullWeeks + b))
$$

The final total deposit amount is $sum = sum1 + sum2$.

The time complexity is $O(1)$ and the space complexity is $O(1)$.

<p><strong>Time Complexity:</strong> $O(1)</p>
<p><strong>Space Complexity:</strong> $O(1)$</p>

<!-- tabs:start -->

```java
class Solution {
    public int totalMoney(int n) {
        int fullWeeks = n / 7, rmngDays = n % 7;
        
        int firstFullWeek = 28;
        int lastFullWeek = 28 + (7 * (fullWeeks - 1));
        
        int sum1 = apSum(firstFullWeek, lastFullWeek, fullWeeks);
        int sum2 = apSum(fullWeeks + 1, fullWeeks + rmngDays, rmngDays);
        
        return (sum1 + sum2);
    }
    
    public int apSum(int a, int b, int count) {
        // Sum of AP (a, ..., b) = (a + b) * count / 2;
        return ((a + b) * count / 2);
    }
}
```

<!-- tabs:end -->

<!-- solution:end -->

<!-- problem:end -->